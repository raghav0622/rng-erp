'use client';

import { OwnerBootstrapScreen } from '@/rng-platform/rng-auth/app-auth-components';

export default function OnboardingPage() {
  return <OwnerBootstrapScreen header={<>Bootstrap The Owner!</>} />;
}
